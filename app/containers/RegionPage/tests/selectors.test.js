// import { selectNuRegionPageDomain } from '../selectors';

describe('selectNuRegionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
