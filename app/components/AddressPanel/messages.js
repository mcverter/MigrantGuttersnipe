/*
 * AddressPanel Messages
 *
 * This contains all the text for the AddressPanel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddressPanel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddressPanel component!',
  },
});
