/*
 * TypePanel Messages
 *
 * This contains all the text for the TypePanel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TypePanel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TypePanel component!',
  },
});
