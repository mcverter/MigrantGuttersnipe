/*
 * ShareableResourcePage Messages
 *
 * This contains all the text for the ShareableResourcePage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ShareableResourcePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ShareableResourcePage component!',
  },
});
