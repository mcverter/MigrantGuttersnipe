/*
 * NotesPanel Messages
 *
 * This contains all the text for the NotesPanel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NotesPanel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NotesPanel component!',
  },
});
